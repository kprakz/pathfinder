# Copy to .env.local.

# Demo mode: serve built-in sample paths instead of calling the API (free, no key needed).
DEMO_MODE=true

# Needed only when DEMO_MODE is not "true". Get a key from https://console.anthropic.com/
ANTHROPIC_API_KEY=
